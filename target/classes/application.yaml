spring :
    application:
        name : student
    datasource:
        url: jdbc:mysql://localhost:3306/bill_payment_platform
        username: root
        password: Root@1234
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate: 
            ddl-auto: update
        show-sql : true
        properties:
            hibernate:
                format-sql: true

logging:
    level:
        org:
            hibernate:
                stat: debug
                type: trace

